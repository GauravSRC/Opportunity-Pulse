export default function SettingsPage() {
  // TODO(phase-1/3): profile editor (PATCH /profiles) + alert settings.
  return (
    <div>
      <h2>Settings &amp; alerts</h2>
      <p style={{ color: "#888" }}>
        TODO(phase-1): edit profile; TODO(phase-3): configure alerts.
      </p>
    </div>
  );
}
