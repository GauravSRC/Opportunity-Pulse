export default function OnboardingPage() {
  // TODO(phase-1): résumé upload + skill/intent/location form -> POST /profiles.
  return (
    <div>
      <h2>Onboarding</h2>
      <p>Upload your résumé and pick skills, intents, and locations.</p>
      <p style={{ color: "#888" }}>TODO(phase-1): wire to POST /profiles.</p>
    </div>
  );
}
