# Frontend (Next.js) environment. Copy to .env.local for local dev.
# On Vercel, set NEXT_PUBLIC_API_BASE_URL in Project Settings -> Environment Variables.

# Base URL of the deployed FastAPI backend (no trailing slash).
# Local:      http://localhost:8000
# Production: https://opportunity-pulse-api.onrender.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
