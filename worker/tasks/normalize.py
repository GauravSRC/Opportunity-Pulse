"""Normalization task — map raw_listings to normalized_listings."""

from __future__ import annotations


async def run_normalize(ctx: dict, raw_ids: list[str] | None = None) -> dict:
    """TODO(phase-1): call ingestion.normalize for new raw listings."""
    raise NotImplementedError("run_normalize")
