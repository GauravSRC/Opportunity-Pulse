# Scripts

Operational and dev helper scripts.

| Script | Purpose |
|---|---|
| `seed_sources.py` | Upsert `ingestion/registry.yaml` into `opportunity_sources` |

TODO(phase-1+): add `run_discovery.py` (manual ingest trigger),
`backfill_embeddings.py` (re-embed after a model change), and
`export_user_data.py` (data rights).
